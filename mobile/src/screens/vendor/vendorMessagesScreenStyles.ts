import { StyleSheet } from "react-native";
import { COLORS, RADIUS, SPACING } from "../../constants/theme";

export const styles = StyleSheet.create({
  safeArea: { flex: 1, backgroundColor: COLORS.background },
  scroll: { flexGrow: 1, padding: SPACING.lg, paddingBottom: 42 },
  header: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: SPACING.xl,
  },
  headerIcon: {
    width: 45,
    height: 45,
    borderRadius: 16,
    backgroundColor: COLORS.primaryLight,
    alignItems: "center",
    justifyContent: "center",
  },
  headerCopy: { flex: 1, minWidth: 0, marginHorizontal: 12 },
  headerTitle: {
    fontSize: 23,
    lineHeight: 30,
    fontWeight: "900",
    color: COLORS.text,
  },
  headerSubtitle: { fontSize: 12, color: COLORS.textMuted, marginTop: 1 },
  countPill: {
    minWidth: 34,
    height: 30,
    paddingHorizontal: 10,
    borderRadius: 15,
    backgroundColor: COLORS.primary,
    alignItems: "center",
    justifyContent: "center",
  },
  countText: { color: COLORS.white, fontSize: 12, fontWeight: "900" },
  listCard: {
    overflow: "hidden",
    borderRadius: 22,
    backgroundColor: COLORS.surface,
    borderWidth: 1,
    borderColor: COLORS.border,
    shadowColor: COLORS.primaryDeep,
    shadowOpacity: 0.07,
    shadowRadius: 10,
    shadowOffset: { width: 0, height: 4 },
    elevation: 2,
  },
  row: {
    flexDirection: "row",
    alignItems: "center",
    padding: 15,
    borderBottomWidth: 1,
    borderBottomColor: COLORS.borderDivider,
  },
  lastRow: { borderBottomWidth: 0 },
  avatar: {
    width: 49,
    height: 49,
    borderRadius: 18,
    backgroundColor: COLORS.primaryLight,
    justifyContent: "center",
    alignItems: "center",
    marginRight: SPACING.md,
  },
  avatarText: { fontSize: 17, fontWeight: "900", color: COLORS.primary },
  content: { flex: 1, minWidth: 0 },
  nameRow: { flexDirection: "row", alignItems: "center" },
  name: { flex: 1, fontSize: 14, fontWeight: "800", color: COLORS.text },
  timestamp: { fontSize: 10, color: COLORS.textLight, marginLeft: 8 },
  messageRow: { flexDirection: "row", alignItems: "center", marginTop: 4 },
  lastMessage: { flex: 1, fontSize: 12, color: COLORS.textMuted, marginRight: 5 },
  stateCard: {
    minHeight: 190,
    borderRadius: 22,
    borderWidth: 1,
    borderStyle: "dashed",
    borderColor: COLORS.borderEmptyState,
    backgroundColor: COLORS.surface,
    alignItems: "center",
    justifyContent: "center",
    padding: 24,
  },
  emptyIcon: {
    width: 64,
    height: 64,
    borderRadius: 22,
    backgroundColor: COLORS.primaryLight,
    alignItems: "center",
    justifyContent: "center",
    marginBottom: 13,
  },
  stateTitle: { color: COLORS.text, fontSize: 15, fontWeight: "900", marginTop: 12 },
  stateText: {
    color: COLORS.textMuted,
    fontSize: 11,
    lineHeight: 17,
    textAlign: "center",
    marginTop: 6,
  },
  errorCard: {
    minHeight: 190,
    borderRadius: RADIUS.xl,
    borderWidth: 1,
    borderColor: "#FBC4C4",
    backgroundColor: "#FFF1F1",
    alignItems: "center",
    justifyContent: "center",
    padding: 24,
  },
  errorIcon: {
    width: 58,
    height: 58,
    borderRadius: 20,
    backgroundColor: "#FFE1E1",
    alignItems: "center",
    justifyContent: "center",
  },
  errorTitle: { color: COLORS.text, fontSize: 15, fontWeight: "900", marginTop: 12 },
  errorText: {
    color: COLORS.danger,
    fontSize: 11,
    lineHeight: 17,
    textAlign: "center",
    marginTop: 6,
  },
  retryText: { color: COLORS.primary, fontSize: 11, fontWeight: "900", marginTop: 9 },
});
